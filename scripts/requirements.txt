# ClawHub 500 自动化脚本依赖

# HTTP 请求
requests>=2.31.0

# AI API (兼容 OpenAI 格式)
openai>=1.12.0
# 阿里云百炼使用 OpenAI 兼容格式

# YAML 解析
pyyaml>=6.0.1

# 日期处理
python-dateutil>=2.8.2

# Tavily API (搜索)
# 通过 requests 直接调用

# VirusTotal API (安全)
# 通过 requests 直接调用
